-- 2517 PRODUTOS
SELECT * FROM DimProduct

SELECT * FROM DimCustomer

-- 18869 REDUZIU CLIENTES

SELECT
	CUSTOMERKEY AS IDCLIENTE,
	FIRSTNAME AS NOME,
	EMAILADDRESS AS EMAIL,
	BIRTHDATE AS ANIVERSÁRIO
FROM
	DimCustomer
-- COLUNAS RENOMEADAS

SELECT
	TOP(100)
	FIRSTNAME,
	EmailAddress,
	BirthDate
FROM
	DimCustomer
-- SELEÇÃO DOS 100 PRIMEIROS + APENAS ALGUMAS COLUNAS

SELECT
	TOP(10)
	PERCENT
* FROM
	DimCustomer
